Supplite project log

Keep this append-only.
Put newest entry at the top.
Use UTC timestamps like: 2026-04-10T12:34:56Z

---
2026-04-10T00:00:00Z | data-pipeline | completed
Files: backend/src/supply_chain_brief/housing_signals.py, backend/scripts/export_real_estate_training_data.py
Technical:
- Fixed national ZCTA centroid loading using Census Gazetteer yearly directory path.
- Added delimiter-aware parsing for centroid ingestion.
- Verified national export path with ACS year fallback.
Plain English:
- Nationwide location loading now works, so full-US points can be exported.
Validation:
- Exported 33772 rows to data/real_estate_training_data_full.csv
Security:
- No secrets added; generated CSV stays local and ignored by git rules.
Next:
- Retrain model after future data refresh.

---
2026-04-10T00:00:00Z | backend | completed
Files: backend/src/supply_chain_brief/main.py, backend/scripts/train_real_estate_model.py, backend/README.md
Technical:
- Added full-dataset training script outputting scored_real_estate_demand_full.csv.
- Updated scored heatmap API notes to use lat/lon from scored CSV.
- Added runbook commands for full-US export and training.
Plain English:
- You can now train and score the nationwide dataset in one flow.
Validation:
- Trained rows: 33772; scored file generated.
Security:
- No credential changes.
Next:
- Add optional model version metadata.

---
2026-04-10T00:00:00Z | frontend | completed
Files: frontend/src/api/realEstateApi.js, frontend/src/components/SciFiMap.jsx
Technical:
- Increased national heatmap fetch limit.
- Kept layer toggles (Heatmap, Stores, DC Radius) for less clutter.
Plain English:
- Map now requests broader US data and still allows clean layer control.
Validation:
- Frontend build passed.
Security:
- No sensitive runtime values exposed.
Next:
- Add viewport-based fetch for large-scale performance.
